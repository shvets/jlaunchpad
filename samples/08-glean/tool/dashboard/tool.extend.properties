checkstyle.data.file  = ${gen.report.root}/checkstyle/${src.project.name}-checkstyle-feedback.xml
cobertura.data.file   = ${gen.report.root}/cobertura/coverage.xml
cpd.data.file         = ${gen.report.root}/cpd/${src.project.name}-cpd-feedback.xml
emma.data.file        = ${gen.report.root}/emma/coverage.xml
pmd.data.file         = ${gen.report.root}/pmd/${src.project.name}-pmd-feedback.xml
findbugs.data.file    = ${gen.report.root}/findbugs/${src.project.name}-findbugs-feedback.xml
junitreport.data.file = ${gen.report.root}/junitreport/TESTS-TestSuites.xml
javancss.data.file    = ${gen.report.root}/javancss/${src.project.name}-javancss-feedback.xml
jdepend.data.file     = ${gen.report.root}/jdepend/${src.project.name}-jdepend-feedback.xml

# Cobertura inserts a DOCTYPE element with an external URL into its data file.
# To allow us to run behind a firewall, we will strip this line out.
cobertura.dtd.spec    = <!DOCTYPE coverage SYSTEM "http://cobertura.sourceforge.net/xml/coverage-02.dtd">

